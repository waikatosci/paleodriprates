# paleodriprates — pip-only install path
#
# This file lists the runtime dependencies needed to run the Dr Paleo
# Flask app and the CLI driver (drip_rate_mc_realisations.py) from a
# plain `pip install -r requirements.txt`.
#
# The recommended install path is conda — see drip_rate.yml. Use this
# file only if you don't want or can't use conda.
#
# Versions are pinned to the same major.minor as drip_rate.yml so the
# behaviour matches the conda environment.

flask>=3.0
matplotlib==3.10.*
numpy==1.26.*
openpyxl==3.1.*
pandas==2.2.*
pillow==11.1.*
progressbar==2.5
scipy==1.11.*
